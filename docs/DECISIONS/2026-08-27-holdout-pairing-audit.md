# Audit — protected holdout pairing

The existing protected holdout executor uses one Arena game per manifest case. The active measurement contract requires colour-inverted pairs sharing the same explicit case seed. This is a correctness issue in the holdout measurement infrastructure, not evidence about engine strength.

The protected manifest remains authoritative and must not be padded into the 16-opening development seed interface.
