# RedWar — A0 Correctness Gate Verdict

## Historical verdict

**A0 = PASS.**

Este documento é histórico e permanece válido para o gate que definiu. Não representa o baseline atual nem substitui [`A01_SEMANTIC_CLOSURE_2026-09-07.md`](A01_SEMANTIC_CLOSURE_2026-09-07.md).

## Baseline histórico

O verdict original referia-se a um `main` anterior. Desde então, novas PRs fecharam partes de A0.1. Portanto, referências a SHA antigos e a problemas entretanto resolvidos devem ser lidas como evidência temporal.

## O que aconteceu depois do A0

- #299 tornou a observação de repetição Python idempotente.
- #300 removeu o bloco FrostMage inalcançável depois de cobertura equivalente.
- #303 estabeleceu special-spell legality parity.
- #306 consolidou `engine.legal_actions` como fronteira canónica.
- #308 normalizou a entrada de `GameState.execute_action()`.
- #310 fez o terminal contract observar os scores do `alpha_beta()` real.
- #313 sincronizou o documento A0.1 com esse estado.

## Estado posterior relevante

A0.1 continua a existir porque dois problemas são arquiteturais e não devem ser inferidos como resolvidos:

1. autoridade de legalidade no executor antes da mutação;
2. contrato nativo para repetition/history.

A partir do baseline atual, usar [`CURRENT_STATE.md`](CURRENT_STATE.md) e [`ROADMAP.md`](ROADMAP.md), não o SHA deste documento.

## Regra

`A0 PASS` nunca significou “Ares forte”, “NNUE melhor” ou “balanceamento correto”. Significou apenas que o gate de correctness definido naquele momento passou.
