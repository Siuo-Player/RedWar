# RedWar — A0 correctness gate verdict — 2026-09-07

## Verdict

**A0 = PASS**

This verdict is scoped to the correctness gate defined by the operational roadmap. It does not claim search-strength improvement, balance improvement, statistical significance, or global AI strength.

## Verified baseline

`main = f7499ae8ab09d1f51b07de0c7737cae106a58dea`

PR `#291` was subsequently merged into `main` after its required Test Suite, AI Quality Gate and CodeQL checks passed. Its changes complete the Python analysis action-space correction, but do not retroactively change the scope of the A0 verdict.

## Gate evidence

| Requirement | Evidence | Result |
|---|---|---|
| C4 lifecycle/TWC/effects/spawn-cooldown/special-spell/terminal coverage | `tests/test_c4_lifecycle_twc_specials.py` plus the existing cross-backend suite | PASS |
| Python/C++ post-action comparison | New bridge fixtures compare `AFTER` RWEN against Python post-state | PASS |
| make → undo → exact root restoration | New fixtures assert `RESTORED` RWEN equals the original root; existing bridge checks root hash/TWC/turn | PASS |
| Fixed node-budget semantics | `tests/test_a0_node_budget_semantics.py` exercises `go nodes N`; production implementation uses an infinite time limit for node-bounded search | PASS |
| Required CI gates | Test Suite green; AI Quality Gate green; CodeQL Python green; CodeQL C++ green on PR `#288` | PASS |

## CI evidence

For PR `#288` at head `ca7253f8e5c9beeccc465be033513c18359462fc`:

- RedWar Test Suite run `34141001300` — success (`409` tests executed).
- RedWar AI Quality Gate run `34141001114` — success.
- RedWar CodeQL run `34141001240` — Python success, C++ success.

The Test Suite's first run exposed an invalid test assumption around `FrostMage.get_valid_stuns()` and was corrected before the green validation run. The final green run is therefore the evidence used for this verdict.

## Scope boundary

A0 PASS does not authorize presenting existing benchmarks as strength evidence, nor does it turn C3/C4 correctness evidence into a strength claim. The subsequent semantic audit identified contracts that still require explicit closure before Strength work is reopened.

The project therefore enters **A0.1 Semantic Closure** before Strength calibration. This is a correctness/architecture tranche, not a dataset/statistical/strength-experiment tranche.
