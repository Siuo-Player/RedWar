# RedWar — A0 correctness gate verdict — 2026-09-07

## Verdict

**A0 = PASS**

This verdict is scoped to the correctness gate defined by the operational roadmap. It does not claim search-strength improvement, balance improvement, statistical significance, or global AI strength.

## Verified baseline

`main = 4078f51a7298b94bab49d0c4af292cf7ad7d0df6`

The baseline includes the merged correctness work through PR `#292` and the roadmap reconciliation PR `#296`.

PR `#291` completed the Python analysis action-space correction. PR `#292` then added the explicit A0.1 semantic-closure contracts for the Inquisitor silence/stun boundary and differential action coverage. PR `#296` reconciled the roadmap against that state.

## Gate evidence

| Requirement | Evidence | Result |
|---|---|---|
| C4 lifecycle/TWC/effects/spawn-cooldown/special-spell/terminal coverage | `tests/test_c4_lifecycle_twc_specials.py` plus the existing cross-backend suite | PASS |
| Python/C++ post-action comparison | Bridge fixtures compare `AFTER` RWEN against Python post-state | PASS |
| make → undo → exact root restoration | Bridge fixtures assert `RESTORED` RWEN equals the original root | PASS |
| Fixed node-budget semantics | `tests/test_a0_node_budget_semantics.py` exercises `go nodes N`; production implementation uses an infinite time limit for node-bounded search | PASS |
| Python full action-space analysis | PR `#291` adds the canonical legal-action adapter covering MOVE/ATTACK/STUN/SPAWN/SPELL with deterministic ordering | PASS |
| Inquisitor silence/stun differential boundary | PR `#292` adds dedicated regressions showing active Inquisitor silence and stunned-Inquisitor exception agree with the C3 oracle | PASS |
| Required CI gates | PR `#288` Test Suite, AI Quality Gate and CodeQL all green; PR `#296` Test Suite, AI Quality Gate and CodeQL all green | PASS |

## CI evidence

For PR `#288` at head `ca7253f8e5c9beeccc465be033513c18359462fc`:

- RedWar Test Suite run `34141001300` — success (`409` tests executed).
- RedWar AI Quality Gate run `34141001114` — success.
- RedWar CodeQL run `34141001240` — Python success, C++ success.

For PR `#296` at head `635149d864be24fee3178092495e2996269ddee2`:

- RedWar Test Suite run `34162642177` — success.
- RedWar AI Quality Gate run `34162642291` — success.
- RedWar CodeQL run `34162642350` — success.

## Scope boundary

A0 PASS does not authorize presenting existing benchmarks as strength evidence, nor does it turn C3/C4 correctness evidence into a strength claim. The subsequent semantic audit identified contracts that still require explicit closure before Strength work is reopened.

The project therefore remains in **A0.1 Semantic Closure** before Strength calibration. Current open correctness boundaries include repetition-history semantics (#294), terminal winner/value differential coverage (#295), and removal of unreachable legacy FrostMage code (#297).

These are correctness/architecture tasks, not dataset/statistical/strength-experiment work.